// Author: Adam Henry, adlhenry@ucsc.edu

COMPILE
	//sudo make -j2 buildkernel -DKERNFAST KERNCONF=MYKERNEL
	sudo make buildkernel KERNCONF=MYKERNEL
	
INSTALL
	sudo make installkernel KERNCONF=MYKERNEL
	sudo shutdown -r now
	
TEST
	

// Author: Adam Henry, adlhenry@ucsc.edu

COMPILE
	//make -j2 buildkernel -DKERNFAST KERNCONF=MYKERNEL
	make buildkernel KERNCONF=MYKERNEL
	
INSTALL
	make installkernel KERNCONF=MYKERNEL
	sudo shutdown -r now
	
TEST
	
